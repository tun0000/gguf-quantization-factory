# Ollama 部署實測紀錄

- 環境:Windows 11 + Ollama 0.31.2,RTX 4090
- 部署了三個變體(同一 model 名下不同 tag):
  - `qwen2.5-3b-local:latest`(Q4_K_M,1.9 GB,[ollama/Modelfile](../ollama/Modelfile))
  - `qwen2.5-3b-local:iq4_xs`(IQ4_XS + 英文 wikitext imatrix,1.7 GB,[ollama/Modelfile.iq4_xs](../ollama/Modelfile.iq4_xs))
  - `qwen2.5-3b-local:iq4_xs-zhtw`(IQ4_XS + 繁中維基 imatrix,1.7 GB,[ollama/Modelfile.iq4_xs_zhtw](../ollama/Modelfile.iq4_xs_zhtw))
- 建立指令:`ollama create qwen2.5-3b-local -f Modelfile` → `success`

## 測試問答(2026-07-11)

**指令:**

```
ollama run qwen2.5-3b-local "請用三句話向初學者解釋什麼是模型量化,以及為什麼在本機跑大型語言模型時需要它?"
```

**模型輸出(原樣記錄):**

> 模型量化是指將模型中的浮点数参数转换为整数(如8位、16位或32位)的过程,以减少模型的计算复杂度和存储需求。对于大型语言模型来说,由于参数量庞大,直接在本地运行时会消耗大量内存和计算资源。通过量化模型,可以显著降低模型大小和运行开销,使模型能够更高效地在本机设备上执行,从而提升用户体验和系统性能。

## IQ4_XS 變體同題實測(2026-07-11)

**指令:**

```
ollama run qwen2.5-3b-local:iq4_xs "請用三句話向初學者解釋什麼是模型量化,以及為什麼在本機跑大型語言模型時需要它?"
```

**模型輸出(原樣記錄):**

> 模型量化是指通過將模型權重和激活值從浮點數(如32位浮點數)轉換為整數(例如8位或16位),來減少模型大小和運行時消耗的存儲空間與計算資源,同時在一定程度上保持模型性能。在本機運行大型語言模型時,量化可以有效降低計算成本和能耗,使得在硬件限制較低的設備上也能運行大型模型,提升了模型的普及性和應用場景的靈活性。

- ✅ Template/stop tokens 正常,內容正確扣題。
- 這次輸出以繁體為主(僅用詞偏陸;對照 Q4_K_M 那次大幅飄簡體)——單次取樣的隨機差異,
  不宜過度解讀為 IQ4_XS 品質較好的證據;可靠的品質比較請看 PPL 數據
  (IQ4_XS +3.40% vs Q4_K_M +5.64%,見 [summary-qwen2.5-3b-instruct.md](summary-qwen2.5-3b-instruct.md))。

## IQ4_XS-ZHTW(繁中 imatrix)變體同題實測(2026-07-11)

**指令:**

```
ollama run qwen2.5-3b-local:iq4_xs-zhtw "請用三句話向初學者解釋什麼是模型量化,以及為什麼在本機跑大型語言模型時需要它?"
```

**模型輸出(原樣記錄):**

> 模型量化是指將大規模模型的權重從浮點數轉換為較小的整數,以減少模型大小和運行開銷。在本地環境運行大型語言模型時,由于計算資源和存儲空間的限制,量化可以大幅降低模型的大小,使得模型能夠在更小的設備上運行,如手機或計算機,而不需要使用大量的存儲空間和計算能力。

- ✅ Template/stop tokens 正常,三句收尾,內容正確扣題。
- ⚠️ 誠實記錄:輸出中出現「由**于**」(簡體用法,繁體應為「由於」)——即使用繁中維基語料
  校正了 imatrix,這個問題依然存在。**這是預期中的結果**:imatrix 校正影響的是量化時
  每個權重的數值精度分配(哪些權重要保留更高精度),不會改變模型在預訓練階段學到的
  語言使用傾向。繁簡混用是 Qwen2.5-3B 這個底層模型的固有特性,量化校正語料的語言
  不會、也不能修正它——這正是本次實驗想驗證、也確實驗證到的边界。
- PPL 數據上,繁中校正版本在繁中 held-out 語料上確實小幅勝出英文校正版本
  (IQ4_XS-ZHTW +3.38% vs IQ4_XS +3.75% vs F16),但差距不大,不足以在單次生成的
  用詞選擇上造成肉眼可辨的差異——量化校正語料的語言更多是「錦上添花」而非決定性因素。

## 觀察

- ✅ ChatML template 與 stop tokens 運作正常:回答完整三句、正常收尾,沒有洩漏 `<|im_start|>` 或無限生成。
- ✅ 內容正確扣題(量化 = 降低參數精度以省記憶體/算力)。
- ⚠️ 有趣的小模型現象:用繁中提問,3B 模型回答會飄向簡體(繁簡混用),
  這是 Qwen2.5-3B 這個尺寸常見的行為,與量化無關(F16 也會);模型能力限制,非部署問題。
- 細節小瑕疵:「如8位、16位或32位」的舉例不夠精確(常見量化是 4~8 位;32 位是原始精度),
  屬 3B 模型的知識精度限制,如實記錄。
